Assembly Code Transpiler


Overview

Write your code in a file.

Provide that file as input to the program.

The program outputs the generated assembly code.

Usage

Clone the repository:

git clone <repository-url>


Open the project in a C-compatible IDE.

Run the program:

gcc main.c -o assembler
./assembler <input-file>
